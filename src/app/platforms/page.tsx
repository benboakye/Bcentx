import { PageShell } from "@/components/PageShell";

export const metadata = {
  title: "Platforms & Tools",
};

export default function PlatformsPage() {
  return (
    <PageShell
      title="Platforms & Tools"
      description="Shell for platform and tool pages. Later sprints will add verified platforms, disclosures, and last-verified dates."
    />
  );
}
