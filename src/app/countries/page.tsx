import { PageShell } from "@/components/PageShell";

export const metadata = {
  title: "Countries",
};

export default function CountriesPage() {
  return (
    <PageShell
      title="Country Availability"
      description="Shell for country hub pages. Later sprints will cover availability, limitations, and cautious country-specific notes."
    />
  );
}
