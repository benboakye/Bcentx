import { PageShell } from "@/components/PageShell";

export const metadata = {
  title: "Learning Centre",
};

export default function BlogPage() {
  return (
    <PageShell
      title="Learning Centre"
      description="Shell for the Learning Centre / blog. Preserves the V1 educational feel while migrating content into a structured, SEO-ready article system."
    />
  );
}
